/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images: {
        domains: ['via.placeholder.com'],
      },
};
// next.config.js
  
export default nextConfig;
